(function(global){
'use strict';
function esc(value){var node=document.createElement('span');node.textContent=String(value??'');return node.innerHTML;}
function roleAllowed(){var c=global.POURGRID_AUTH_CONTEXT?.capabilities||{};return !!(c.admin||c.manage||c.approve);}
async function open(){var modal=document.getElementById('pgSeasonalModal');if(!modal){modal=document.createElement('div');modal.id='pgSeasonalModal';modal.className='pg-team-backdrop';modal.innerHTML='<section class="pg-team-dialog" role="dialog" aria-modal="true" aria-labelledby="pgSeasonalTitle"><header><div><small>MANAGER TOOLS</small><h2 id="pgSeasonalTitle">Seasonal Profiles</h2><p>Temporary build-to multipliers without changing base targets</p></div><button type="button" data-close aria-label="Close">×</button></header><div id="pgSeasonalBody">Loading…</div></section>';document.body.appendChild(modal);modal.querySelector('[data-close]').onclick=function(){modal.hidden=true;};modal.onclick=function(e){if(e.target===modal)modal.hidden=true;};}modal.hidden=false;await render(modal.querySelector('#pgSeasonalBody'));}
async function render(body){try{var profiles=await global.POURGRID_SEASONAL_API.list(),effective=profiles.find(function(p){return p.is_effective;});body.innerHTML='<div class="pg-team-note"><strong>Effective profile: '+esc(effective?.name||'Normal')+' · '+esc(effective?.percentage_multiplier||100)+'%</strong><br>Base build-tos are never overwritten. New drafts pin the effective profile and adjusted targets; existing drafts do not recalculate.</div>'+profiles.map(function(p){return '<article class="pg-team-item"><div><strong>'+esc(p.name)+'</strong><span>'+esc(p.profile_type)+' · '+esc(p.percentage_multiplier)+'% · '+esc(p.status)+'</span></div></article>';}).join('')+(roleAllowed()?'<form id="pgSeasonalForm"><label>New inactive profile</label><input id="pgSeasonalName" required maxlength="80" placeholder="Offseason"><select id="pgSeasonalType"><option>Offseason</option><option>Peak season</option><option>Event week</option><option>Custom</option></select><input id="pgSeasonalPct" type="number" min="50" max="150" step="5" value="100" required><textarea id="pgSeasonalNote" placeholder="Optional note"></textarea><button type="submit">Create inactive profile</button><div id="pgSeasonalMessage" role="status"></div></form>':'<p>Manager or administrator access is required to change profiles.</p>');var form=document.getElementById('pgSeasonalForm');if(form)form.onsubmit=async function(e){e.preventDefault();var button=e.submitter,message=document.getElementById('pgSeasonalMessage'),pct=Number(document.getElementById('pgSeasonalPct').value);if(pct<50||pct>150){message.textContent='Enter a percentage from 50% to 150%.';return;}button.disabled=true;try{await global.POURGRID_SEASONAL_API.save({name:document.getElementById('pgSeasonalName').value.trim(),profileType:document.getElementById('pgSeasonalType').value,percentageMultiplier:pct,note:document.getElementById('pgSeasonalNote').value.trim(),status:'inactive',calculationVersion:global.PourGridHistory.SEASONAL_VERSION});await render(body);}catch(error){message.textContent='The profile could not be saved. Check authorization, dates, and percentage.';}finally{button.disabled=false;}};}catch(error){body.textContent='Seasonal Profiles could not be loaded. Retry when the secure connection is available.';}}
function install(){if(!global.POURGRID_SEASONAL_API||document.getElementById('pgSeasonalTrigger'))return;var home=document.querySelector('.pg-home-body')||document.querySelector('.screen');if(!home)return;var button=document.createElement('button');button.id='pgSeasonalTrigger';button.className='pg-estimator-trigger';button.innerHTML='<div><strong>Seasonal Profiles</strong><span>Normal 100% is the safe default · review inactive and scheduled profiles</span></div><b aria-hidden="true">›</b>';button.onclick=open;home.appendChild(button);}
new MutationObserver(function(){install();}).observe(document.body,{subtree:true,childList:true});setTimeout(install,0);global.pgOpenSeasonalProfiles=open;
})(window);
